name: Scheduler Probe

on:
  pull_request:
    branches: [main]
    paths:
      - 'jobs/scheduler-probe/**'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: |
          date -u
          echo scheduler-ok
